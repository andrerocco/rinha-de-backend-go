package pessoa

// O arquivo service.go contém a definição da interface Service e a implementação do serviço de pessoa pessoaService.
// O serviço atua como uma camada intermediária entre os controladores (handlers), que lidam com as solicitações HTTP, e o
// repositório (repository), que interage com o banco de dados.

type Service interface {
}
